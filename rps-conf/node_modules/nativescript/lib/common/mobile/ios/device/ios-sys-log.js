"use strict";
require(process.argv[2]);
const fiberBootstrap = require("../../../fiber-bootstrap");
const ios_core_1 = require("./ios-core");
fiberBootstrap.run(() => {
    let winSocket = $injector.resolve(ios_core_1.WinSocket, { service: process.argv[3], format: process.argv[4] });
    winSocket.readSystemLogBlocking();
});
